#ifndef UTILS_H
#define UTILS_H

// join_strings concatenates the strings in the given array into a single
// string. Author: ChatGPT.
char *join_strings(char *strings[], int start, int end);

#endif
